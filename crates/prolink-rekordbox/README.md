# prolink-rekordbox

Readers for a rekordbox device export: `export.pdb`, the `ANLZ####.DAT`/`.EXT`
analysis files, and `PIONEER/*SETTING*.DAT` — plus the joined library model the
Pro DJ Link browse surface is built from.

Part of [prolink](https://github.com/usr-ein/prolink).
